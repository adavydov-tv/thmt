apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dashboard.name" . }}-env
  labels: {{- include "dashboard.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.dashboard.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
